---
id: terminal-init-spawn
name: Terminal Init — Spawn
version: 1.0.0
summary: First terminal WebSocket frame requesting a fresh agent terminal.
owners:
  - ticketry
badges:
  - content: WebSocket
    backgroundColor: purple
    textColor: purple
---

`type: init` and `mode: spawn` are explicit discriminants. The full generated
wire schema is maintained at
`studio/src/shared/api/transport/wire-frames.schema.json`.
