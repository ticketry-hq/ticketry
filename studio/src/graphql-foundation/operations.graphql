query FoundationProbe {
  migrationProbes {
    nodes {
      id
      value
    }
  }
}

mutation CreateMigrationProbe($data: MigrationProbesInsertInput!) {
  migrationProbesCreateOne(data: $data) {
    id
    value
  }
}
