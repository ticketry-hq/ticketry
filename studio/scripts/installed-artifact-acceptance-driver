#!/bin/sh
set -eu

runtime=${MUXED_DESKTOP_ACCEPTANCE_NODE:?MUXED_DESKTOP_ACCEPTANCE_NODE is required}
driver_dir=$(/usr/bin/dirname "$0")
exec "$runtime" "$driver_dir/installed-artifact-acceptance-driver.mjs" "$@"
