{
  "schemaVersion": 2,
  "sourceOfTruth": {
    "artifact": "studio/src-tauri/resources/work-management/reviewed_defaults.json",
    "consumers": [
      "AGENTS.md",
      "review-workbench",
      "studio/scripts/release-build.mjs"
    ]
  },
  "guidance": "# Ticketry desktop application\n\nKeep user-facing responses very concise. Lead with the direct answer or blocker;\nadd detail only when the user asks for it.\n\nThis repository owns the complete Ticketry desktop application: the React\nfrontend, Tauri shell, in-process Rust services, MCP listener, and generated\nGraphQL contracts required by that application.\n\nUse `npm run desktop:dev` or `pnpm run dev` from the repository root for local\ndesktop development. Both commands rebuild and launch Ticketry. Keep browser-\nonly service commands as supporting development tools, not as a separate product.\n\nCode structure is a governing constraint, not a preference: keep files small\nand single-purpose, place frontend code in `studio/src/features/<domain>/`\n(with `app/` for the shell and `shared/` for cross-feature plumbing only), and\ngive each Rust backend capability its own focused module under\n`studio/src-tauri/src/`, with database-backed GraphQL composed through\nSeaORM and Seaography.\nWhen a file outgrows one concern, split it rather than extend it. The full\nrules live in [`CLAUDE.md`](CLAUDE.md) under \"Code structure — governing\nrules\"; keep the two documents consistent.\n\nDatabase-backed GraphQL Models are migration-first and generated-contract-first.\nStart with generated Seaography CRUD and caller-specific GraphQL operations.\nEach public write must bind a concrete identity and allowlist only the fields\nthat the caller may change. If unrestricted generated CRUD would expose protected\nfields or bypass a Ticketry invariant, keep that mutator private and expose one\nrestricted, model-shaped create/update/delete seam instead. Keep validation,\nlocking, revisions, derived-field repair, cascades, and event planning behind\nthat seam.\n\nDo not replace model CRUD with per-field or per-relationship RPCs. In\nparticular, WorkItem parent, blocker, classification, archive, and state requests\nenter through the restricted WorkItem update contract; hierarchy, dependency,\ntransition, and revision code remains internal and transactional. Only behavior\nthat cannot be represented as model CRUD may become a named domain operation,\nand it must be recorded in the route/operation registry with the reason. The\ncurrent exceptions are work-item reorder, state reorder, issue-type reorder,\nremove-state-from-workflow, and onboarding acknowledgement. Do not add\nreplacement CRUD, DAO/repository layers that mirror SeaORM, mirrored DTOs,\n`mutation: false`, or generated-file patches without a written exception that\nidentifies the missing behavior, rejected framework/database facilities, the\nsmallest custom seam, and its drift-prevention test.\n\nTicketry has no product REST API. Browser development may use the Rust GraphQL\nadapter, but it must not grow into a second backend or external compatibility\ncontract.\n\nKeep the Tauri/webview boundary narrow. The native terminal renderer consumes a\npinned libghostty revision through its C API, while tmux remains responsible for\ndurable sessions. Preserve the existing fallback unless a deliberate migration\nremoves it.\n\nDevelopment data must remain isolated from live application data. Generated\ndatabases, caches, native libraries, and build output must not be\ncommitted.\n\nDevelopment frontend, Rust runtime, and MCP output is persisted at\n`.ticketry-dev/logs/ticketry.log`. Use `npm run logs` to inspect recent output,\n`npm run logs:follow` while reproducing a problem, and `npm run logs:clear` to\nstart a clean capture. This directory is generated and must not be committed.\n\nEvery user-visible Studio UI behavior change must add or update an automated\nacceptance case in `studio/src/test/*Acceptance.test.tsx`. Keep the numbered\noverhaul gate current and run `npm run test:overhaul --workspace\n@worktracker/studio` before handing the change off.",
  "states": [
    {
      "name": "Ideas",
      "group": "backlog",
      "color": "#60646C",
      "autoStart": true
    },
    {
      "name": "Grill",
      "group": "backlog",
      "color": "#FA4D56",
      "autoStart": false
    },
    {
      "name": "Spec",
      "group": "unstarted",
      "color": "#8E4EC6",
      "autoStart": true
    },
    {
      "name": "Tickets",
      "group": "unstarted",
      "color": "#33B1FF",
      "autoStart": true
    },
    {
      "name": "Implement",
      "group": "started",
      "color": "#F59E0B",
      "autoStart": false
    },
    {
      "name": "Review",
      "group": "started",
      "color": "#08BDBA",
      "autoStart": false
    },
    {
      "name": "Done",
      "group": "completed",
      "color": "#46A758",
      "autoStart": false
    },
    {
      "name": "Cancelled",
      "group": "cancelled",
      "color": "#9AA4BC",
      "autoStart": false
    }
  ],
  "issueTypes": [
    "Story",
    "PathFind",
    "Implementation"
  ],
  "requiredSkills": {
    "Ideas": [],
    "Grill": [
      "grill-with-docs"
    ],
    "Spec": [
      "to-spec"
    ],
    "Tickets": [
      "to-tickets"
    ],
    "Implement": [],
    "Review": [],
    "Done": [],
    "Cancelled": []
  },
  "prompts": {
    "Story": {
      "Ideas": "This Story is in `Ideas`. Gather enough context to choose its delivery route. Read the title, description, attachments, linked material, and relevant repository context. If the work is small, unambiguous, and self-contained, call the MCP server's `run_now` tool so it moves the Story to `Implement` and launches the implementation run as one operation; never use a bare state move for this route. If essential product or behavior decisions are missing or ambiguous, move the Story to `Grill` using the MCP server and stop without inventing those decisions. If the available context is sufficient for the normal delivery workflow, move the Story to `Spec` using the MCP server so the Spec, Tickets, and Implement pipeline can continue. Do not implement the work or create implementation tickets while in `Ideas`. After making the transition or calling `run_now`, terminate the current agent session using the MCP server.",
      "Grill": "This task is in `Grill`.\n\nUse the `$grill-with-docs` skill to interview the person.\n\nWhen this is complete, move this Story to `Spec` using the MCP server.\n\nOnce you are done, kill the current coding agent session with the MCP too.",
      "Spec": "This task is in `Spec`.\n\nUse the `to-spec` skill to turn the agreed requirements into the Story's implementation specification and link the finished spec from the Story. Do not create implementation tickets in this stage.\n\nWhen this is complete, move this Story to `Tickets` using the MCP server.\n\nOnce you are done, kill the current coding agent session with the MCP too.",
      "Tickets": "This task is in `Tickets`.\n\nUse the `to-tickets` skill to split the approved specification into scoped Implementation children and record their dependency edges. Do not implement any ticket in this stage.\n\nWhen this is complete, move this Story to `Implement` using the MCP server.\n\nOnce you are done, kill the current coding agent session with the MCP too.",
      "Implement": "This task is in `Implement`:\n\nThe implementation campaign is running across your Implementation children; your job is coordination and integration, not re-implementing the children. Do not move the story yourself - it advances to `Review` on its own once every Implementation child is terminal and at least one is `Done`. Surface cross-child integration problems as they appear.\n\nIf there are no Implementation children, first confirm this is still a small, unambiguous direct change. If it is larger or more ambiguous, stop without guessing and move the Story to `Grill` using the MCP server so it can rejoin refinement. Otherwise implement the Story itself and move it to `Review` using the /tdd skill.\n\nFor dependencies, treat `Review` state as unblocked.\n\nAfter this terminate this session using the MCP server with the terminate_current_run tool.",
      "Review": "This task is in `Review`. **What you do depends on this ticket's Type.**\n\n**If this is an Implementation child:** run `code-review` over this child's changes and report findings plainly. The review deliverable is the findings; completing it does not itself request or trigger a move to `Done`.\n\n**If this is a Story:** review the *combined* result of all children together, looking for integration issues that per-child review cannot catch. Turn each actionable finding into a new **Implementation** child through the dedicated `create_review_finding` tool - it creates the child directly in `Implement`, parented to this Story, carrying a fixed `Path` (repo-relative file) / inclusive `Lines` (start-end) / optional `Note` evidence block, so fixes rejoin the same execution machinery. Do not draw a `blocked_by` dependency edge and do not fix findings inline here; implementation waits for a person's kickoff outside this review deliverable. ",
      "Done": "Do nothing!",
      "Cancelled": "Do nothing."
    },
    "PathFind": {
      "Ideas": "This PathFind work item is in `Ideas`, which is not part of the reviewed PathFind workflow. Do not mutate the work item or begin an investigation. Report the workflow mismatch and terminate the current agent session using the MCP server.",
      "Grill": "This task is in `Grill`.\n\nUse the `grill-with-docs` skill to interview the person until the work item's requirements, constraints, and acceptance boundary are explicit. Record the agreed requirements on the work item. Do not write the specification or create implementation tickets in this stage.\n\nWhen grilling is complete, move this work item to `Spec`.",
      "Spec": "This task is in `Spec`.\n\nUse the `to-spec` skill to complete the PathFind investigation and record its findings and decision as the specification deliverable. Do not create implementation tickets in this stage.\n\nWhen the PathFind deliverable is complete, move this work item to `Done`.",
      "Tickets": "This task is in `Tickets`.\n\nUse the `to-tickets` skill to split an approved specification into scoped Implementation children and record their dependency edges. Do not implement any ticket in this stage.\n\nWhen the ticket set is complete, stop and wait for a person to move the work item to `Implement`; that transition is deliberately human-only.",
      "Implement": "This task is in `Implement`: **What you do depends on this ticket's Type.**\n\n**If this is an Implementation child:** implement only this child's agreed slice, from its spec and the parent's HLD. Keep changes local, avoid unrelated edits, and validate the touched behaviour before finishing. You are running because your dependencies are satisfied - do not start work that a `blocked_by` edge still gates. When the slice is complete and validated, move **this child** to `Review` so the review step is triggered by the work itself. If you are blocked, say so and leave it in `Implement`.\n\n**If this is a Story:** the implementation campaign is running across your Implementation children; your job is coordination and integration, not re-implementing the children. Do not move the story yourself - it advances to `Review` on its own once every Implementation child is terminal and at least one is `Done`. Surface cross-child integration problems as they appear.\nIf there are no implementation stories under this, then implement the story itself.\n\nFor dependencies, treat 'Review' state as unblocked.",
      "Review": "This task is in `Review`. **What you do depends on this ticket's Type.**\n\n**If this is an Implementation child:** run `code-review` over this child's changes and report findings plainly. The review deliverable is the findings; completing it does not itself request or trigger a move to `Done`.\n\n**If this is a Story:** review the *combined* result of all children together, looking for integration issues that per-child review cannot catch. Turn each actionable finding into a new **Implementation** child through the dedicated `create_review_finding` tool - it creates the child directly in `Implement`, parented to this Story, carrying a fixed `Path` (repo-relative file) / inclusive `Lines` (start-end) / optional `Note` evidence block, so fixes rejoin the same execution machinery. Do not draw a `blocked_by` dependency edge and do not fix findings inline here; implementation waits for a person's kickoff outside this review deliverable. Final acceptance must be explicitly requested; do not infer it from a clean review. It requires a PR linked to the story. When final acceptance is requested, finalize atomically: commit the worktree changes, open a PR, link it to the story, clean up the worktree, and only then request the `Review -> Done` transition; if any step fails, stay in `Review` and report the exact error rather than advancing.",
      "Done": "Do nothing!",
      "Cancelled": "Do nothing."
    },
    "Implementation": {
      "Ideas": "This Implementation work item is in `Ideas`, which is not part of the reviewed Implementation workflow. Do not mutate the work item or begin implementation. Report the workflow mismatch and terminate the current agent session using the MCP server.",
      "Grill": "This task is in `Grill`.\n\nUse the `grill-with-docs` skill to interview the person until the work item's requirements, constraints, and acceptance boundary are explicit. Record the agreed requirements on the work item. Do not write the specification or create implementation tickets in this stage.\n\nWhen grilling is complete, move this work item to `Spec`.",
      "Spec": "This task is in `Spec`.\n\nUse the `to-spec` skill to turn the agreed requirements into an implementation specification and link the finished spec from the work item. Do not create implementation tickets in this stage.\n\nWhen the specification is complete, move this work item to `Tickets`.",
      "Tickets": "This task is in `Tickets`.\n\nUse the `to-tickets` skill to split the approved specification into scoped Implementation children and record their dependency edges. Do not implement any ticket in this stage.\n\nWhen the ticket set is complete, stop and wait for a person to move the work item to `Implement`; that transition is deliberately human-only.",
      "Implement": "This task is in `Implement`: \n\nOnly this child's agreed slice, from its spec and the parent's HLD. \nKeep changes local, avoid unrelated edits, and validate the touched behaviour before finishing. \n\nYou are running because your dependencies are satisfied - do not start work that a `blocked_by` edge still gates. If the story is in 'Review' then it's not a blocker.\n\nWhen the slice is complete and validated, move **this child** to `Review` so the review step is triggered by the work itself. \nIf you are blocked, say so and leave it in `Implement` and don't terminate yourself.\n\nDon't run any reviews as review would be done in the next stage.\n\nAfter this terminate this session using the MCP server with the terminate_current_run tool.",
      "Review": "This task is in `Review`. \n\nRun `code-review` over this child's changes and report findings plainly. The review deliverable is the findings; Create implementation child through the dedicated `create_review_finding` tool - it creates the child directly in `Implement`, parented to this Story, carrying a fixed `Path` (repo-relative file) / inclusive `Lines` (start-end) / optional `Note` evidence block, so fixes rejoin the same execution machinery. Do not draw a `blocked_by` dependency edge and do not fix findings inline here; implementation waits for a person's kickoff outside this review deliverable.",
      "Done": "Do nothing!",
      "Cancelled": "Do nothing."
    }
  },
  "workflows": {
    "Story": {
      "start": "Ideas",
      "states": [
        "Ideas",
        "Grill",
        "Spec",
        "Tickets",
        "Implement",
        "Review",
        "Done",
        "Cancelled"
      ],
      "transitions": [
        [
          "Ideas",
          "Grill",
          {
            "agentAllowed": true
          }
        ],
        [
          "Ideas",
          "Spec",
          {
            "agentAllowed": true
          }
        ],
        [
          "Ideas",
          "Implement",
          {
            "agentAllowed": true
          }
        ],
        [
          "Grill",
          "Ideas",
          {
            "agentAllowed": true
          }
        ],
        [
          "Grill",
          "Spec",
          {
            "agentAllowed": true
          }
        ],
        [
          "Grill",
          "Cancelled",
          {
            "agentAllowed": true
          }
        ],
        [
          "Spec",
          "Tickets",
          {
            "agentAllowed": true
          }
        ],
        [
          "Spec",
          "Cancelled",
          {
            "agentAllowed": true
          }
        ],
        [
          "Tickets",
          "Implement",
          {
            "agentAllowed": false
          }
        ],
        [
          "Tickets",
          "Cancelled",
          {
            "agentAllowed": true
          }
        ],
        [
          "Implement",
          "Grill",
          {
            "agentAllowed": true
          }
        ],
        [
          "Implement",
          "Review",
          {
            "agentAllowed": true
          }
        ],
        [
          "Implement",
          "Cancelled",
          {
            "agentAllowed": true
          }
        ],
        [
          "Review",
          "Implement",
          {
            "agentAllowed": true
          }
        ],
        [
          "Review",
          "Done",
          {
            "agentAllowed": true
          }
        ],
        [
          "Review",
          "Cancelled",
          {
            "agentAllowed": true
          }
        ]
      ]
    },
    "PathFind": {
      "start": "Spec",
      "states": [
        "Spec",
        "Done",
        "Cancelled"
      ],
      "transitions": [
        [
          "Spec",
          "Done",
          {
            "agentAllowed": true
          }
        ],
        [
          "Spec",
          "Cancelled",
          {
            "agentAllowed": true
          }
        ]
      ]
    },
    "Implementation": {
      "start": "Implement",
      "states": [
        "Implement",
        "Review",
        "Done",
        "Cancelled"
      ],
      "transitions": [
        [
          "Implement",
          "Review",
          {
            "agentAllowed": true
          }
        ],
        [
          "Implement",
          "Cancelled",
          {
            "agentAllowed": true
          }
        ],
        [
          "Review",
          "Implement",
          {
            "agentAllowed": true
          }
        ],
        [
          "Review",
          "Done",
          {
            "agentAllowed": true
          }
        ],
        [
          "Review",
          "Cancelled",
          {
            "agentAllowed": true
          }
        ]
      ]
    }
  },
  "finalizedAt": "2026-07-30T15:30:48.833Z"
}
