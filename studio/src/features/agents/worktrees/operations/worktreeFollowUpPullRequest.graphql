mutation WorktreeFollowUpPullRequest($taskId: String!, $operationId: String!) {
  worktree_pull_request_follow_up(task_id: $taskId, operation_id: $operationId) {
    operation_id
    url
    branch
    base_branch
    pushed
    uncommitted_work_excluded
  }
}
