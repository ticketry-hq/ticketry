---
id: agent-lifecycle-reported
name: Agent Lifecycle Reported
version: 1.0.0
summary: Provider adapter reports a normalized lifecycle event for a durable agent run.
owners:
  - ticketry
operation:
  method: POST
  path: /api/lifecycle/events
  statusCodes:
    - "202"
---

Fields include `agent_run_id`, provider, event `kind`, timestamp, optional
message, provenance source, and optional provider session id.
