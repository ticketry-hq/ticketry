---
id: change-work-item
name: Change Work Item
version: 1.0.0
summary: Mutate authoritative work-item fields or move the item through its workflow.
owners:
  - ticketry
operation:
  method: PATCH
  path: /api/work-tracker/work-items/{work_item_id}
  statusCodes:
    - "200"
    - "404"
    - "409"
    - "422"
---

The HTTP response is authoritative for the caller. A workflow-state identity
change also allocates a project revision and can produce
[[event|work-item-state-changed]]. Other field changes currently do not.
